# 桌面自动化助手 - 核心依赖
# Python 3.11+

# GUI
PySide6>=6.6

# 桌面自动化
pyautogui>=0.9.54
pyperclip>=1.8.2
pywinauto>=0.6.8
psutil>=5.9.0
pywin32>=306

# 图像处理
Pillow>=10.0

# MCP (AI 客户端接入)
mcp>=1.0

# 可选: UPX 路径(打包时压缩,不影响运行)
# pyinstaller>=6.0